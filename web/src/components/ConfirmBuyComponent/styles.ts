import styled from "styled-components";

export const MainComponent = styled.div`
    
    position: absolute;
    z-index: 1000;

    width: 100vw;
    height: 100vh;

    background: rgba(0, 0, 0, .1);
    filter: blur(5px);
`