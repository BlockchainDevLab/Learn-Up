import styled from "styled-components";

export const CoursesComponent = styled.div`
    display: flex;
    flex-flow: row wrap;
    justify-content: center;
    padding: 5rem;
    gap: 2rem;

    min-height: 100%;
`